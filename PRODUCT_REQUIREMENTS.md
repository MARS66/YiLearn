# YiStudy 彝文识字微信小程序产品设计说明

> 文档版本：V2.1（实现版）
> 更新日期：2026-09-17
> 产品形态：纯前端微信小程序（TypeScript + Sass，skyline 渲染，glass-easel 组件框架）
> 数据规模：1,164 个凉山规范彝文音节
>
> **版本说明**：本版以 V2.0 需求为基础，依据当前代码实现逐条核对后更新，删除与实现不符的规划内容，补充已经落地的功能（单字/词组发音、学习卡音调、成就体系、本地存储说明页等）。所有“已实现”条目均有对应代码位置。

## 1. 产品概述

YiStudy 是一款面向零基础用户的轻量彝文识字工具。产品以“每天认识 10 个字”为核心体验。每组学习和每组复习均固定为 10 个字，不提供数量选择。新字按照字典固定顺序依次学习，学习记录保存在当前微信设备本地。

产品不设置课程课节、考试、过关和强制复习。用户可以在需要时，从已经学习的文字中随机抽取若干字进行自主复习。

产品表达统一使用“认识”“学习”“回顾”和“熟悉”，不使用无法通过产品行为验证的“掌握”。

### 1.1 产品目标

1. 用户首次进入后 30 秒内开始认识第一个彝文字。
2. 每组固定学习 10 个字，同时允许用户随时结束并保存已学习进度。
3. 1,164 个彝文字全部可按固定顺序学习、检索和查看详情。
4. 通过清晰的累计进度、连续学习记录和低压力复习，提高次日及 7 日留存。
5. 核心识字与字典功能在离线状态下可用，不要求注册登录。

### 1.2 非目标

V2.x 不包含：

- 课程、章节和课节体系。
- 答题巩固、成绩、正确率、过关和错题本。
- 收藏和收藏列表。
- “还不熟”“记住了”“再记一次”等熟练度判断。
- 自动复习队列、到期复习和间隔记忆算法。
- 账号体系、云端同步、社区、排行榜、在线课程和用户投稿。
- 聊天、会员支付和运营后台。

卸载小程序、清理微信缓存或更换设备后，学习记录可能丢失。

## 2. 用户与核心场景

### 2.1 目标用户

- 想从零开始认识彝文的普通用户。
- 对彝族语言、文字和文化感兴趣的用户。
- 希望利用碎片时间轻松识字、不希望承受考试压力的学习者。

### 2.2 核心场景

- 用户每天打开小程序，从上次位置继续认识新字。
- 用户每天从上次位置继续学习一组 10 个字。
- 用户学习过程中随时退出，下次从未学习的第一个字继续。
- 用户从已学习文字中随机抽取若干字，通过点击揭晓资料进行回顾。
- 用户按彝文字、拼音或文字结构查询单字，并收听字音与词组读音。

## 3. 产品原则

1. **识字优先**：首页只有一个最强主任务——继续认识新字。
2. **推荐而不强迫**：10 个字是推荐目标，不是完成门槛。
3. **随时可结束**：学习一个字也会保存，不产生失败状态。
4. **信息完整、层级清晰**：学习卡提供完整资料，但字形和拼音始终是视觉中心。
5. **复习完全自主**：不提醒欠债、不显示红点、不自动安排复习日期。
6. **进度真实**：只统计用户已经浏览学习的文字，不声称用户已经掌握。
7. **顺序稳定**：新字严格按照字典顺序出现，同一份数据始终得到相同学习路径。

## 4. 信息架构

### 4.1 TabBar 页面

| Tab | 页面职责 | 主要入口 |
| --- | --- | --- |
| 学习 | 今日学习、继续识字、累计进度、复习巩固入口、成就入口、本周打卡 | 继续学习、复习巩固 |
| 字典 | 检索全部 1,164 个字并查看文字详情 | 单字详情 |
| 我的 | 学习数据、连续记录、成就和设置 | 数据说明、本地数据管理 |

产品只保留“学习、字典、我的”三个 Tab，不设置课程或基础教学模块。产品中不使用“课程”“章节”“课节”和“解锁”等概念。

### 4.2 非 TabBar 页面

| 页面 | 页面职责 |
| --- | --- |
| 学习页（learn） | 按字典顺序展示新字的记忆卡片；或随机抽取已学文字的复习卡片 |
| 完成页（complete） | 学习模式生成每日成就海报；复习模式展示本次回顾数量 |
| 单字详情（detail） | 展示单个彝文字的完整资料与学习状态 |
| 成就（achievements） | 展示识字与连续学习共 9 枚徽章 |
| 本地存储说明（storage-info） | 说明本地存储内容并支持清除记录 |

所有非 TabBar 页面使用 `navigateTo` 进入并保留原生返回手势；TabBar 页面之间只使用 `switchTab`。

## 5. 顺序识字体系

### 5.1 学习顺序

- 1,164 个文字按照 `miniprogram/data/words.ts` 的固定顺序依次学习（顺序即 Unicode 彝文音节区段 U+A000–U+A48C 的码表顺序，44 声母 × 10 韵母 × 4 声调，跳过空位）。
- 每个文字在顺序学习主线中只出现一次。
- 系统保存用户已经学习到的字典位置（`nextWordIndex`）。
- 下次开始学习时，从第一个未学习文字继续。
- 同一天多次进入或退出，不得改变推荐的起始位置。
- 字典浏览和单字详情查看不得改变顺序学习进度。
- 用户学习完全部 1,164 个字后，首页主任务改为“开始回顾”（进入随机复习）。

### 5.2 每组数量

- 每组学习固定包含 10 个字，不提供数量选择或自定义入口。
- 用户学习到本组第 10 个字后，底部按钮变为“完成学习”，点击进入完成页，可选择保存海报、继续学习下一组或返回。
- 全部未学习文字不足 10 个时，最后一组使用实际剩余数量。
- 用户提前退出时，已学习文字仍然保存，不显示失败或未完成惩罚。
- 用户当天再次进入时，首页显示当天累计学习数量，并从下一个未学习文字继续。
- 学完最后一个文字后，学习页底部不再显示“下一个”，改为“完成全部识字”。

### 5.3 学习记录时机

- 用户首次进入某张新字学习卡时，将该字记为已学习（`markWordLearned`），同时推进 `nextWordIndex`。
- 同一文字重复查看不得重复增加累计学习字数。
- 点击“上一个”只用于回看，不改变已学习状态和累计数据。
- 写入失败时提示“学习记录保存失败，请检查微信存储空间”，但不阻止当前浏览。

## 6. 核心功能需求

### 6.1 学习 Tab（首页）

首页优先展示当天唯一主任务，避免多个同等级按钮。

已实现内容：

- 今日日期（`M月D日`）和按时段变化的问候语（早上好/中午好/下午好/晚上好）及随机鼓励语。
- 连续学习天数徽标（`连续学习 N 天`）。
- 待续学习第一个字的预览记忆卡：拼音（声母红 / 韵母黄 / 声调灰）+ 超大字形。
- 已认识字数：`已认识 X / 1,164`（在学习卡头部）。
- 今日学习进度：`今日完成 X%` + 10 格进度条。
- 主按钮“开始学习 / 继续学习 / 开始回顾”（学完全部后为“开始回顾”）。
- 复习巩固入口（次级视觉层级，快捷卡）。
- 成就入口（快捷卡，展示最新解锁的连续学习徽章）。
- 当前连续学习天数与本周学习打卡记录（周一至周日，未打卡日显示日期号）。

首页不得出现：

- 待复习数量和催促红点。
- 正确率、考试成绩和错题数量。
- 课程、课节、解锁和待加强状态。

### 6.2 学习页

每张学习记忆卡完整展示：

- 超大彝文字形。
- 完整拼音，由不同颜色的 `声母(红) + 韵母(黄) + 声调(灰)` 组合，位于字形上方。
- 音调名称：高平调（t）/ 次高调（x）/ 低平调（p）/ 中平调（无）。
- 部首。
- 总笔画。
- 常用词组（每次进入卡片随机抽取一条，支持“换一组”随机更换，支持点击播放词组读音）。
- 单字发音：卡片右上角喇叭按钮播放该字真人发音 MP3。
- 本组学习进度，例如“2 / 10”，以及百分比进度条。
- 模式标识与鼓励语：学习模式“正在学习”，复习模式“巩固复习”。

底部操作：

- 上一个。
- 下一个（文案随状态变化：下一个 / 完成学习 / 完成全部识字；复习模式为：下一个 / 完成回顾）。

交互规则：

- 第一张卡的“上一个”不可用（置灰）。
- 点击“下一个”进入字典顺序中的下一文字。
- 用户可以通过原生返回随时退出，当前位置和已学习文字实时保存。
- 达到本组第 10 个字时按钮变为“完成学习”，不强制离开学习页。
- 学完最后一个文字后，不再显示“下一个”，改为“完成全部识字”。
- 复习模式：卡片默认“未揭晓”，只显示字形与“点击卡片查看读音与资料”提示；点击卡片揭晓完整资料并触发反馈。

### 6.3 今日学习完成（学习完成页）

完成一组学习后进入，生成每日学习成就海报：

- 品牌“彝学 YiLearn / 每天认识 10 个彝文字”。
- 生成时间。
- 今日学习字数（大字）。
- 连续学习天数、累计识字、今日识字。
- 本周学习记录（周一至周日打卡）。
- 小程序码与励志文案（“你的每一步，都在延续彝文”）。

操作：

1. 主按钮“保存海报”（当前仅提示“海报已保存到相册”，未落地相册写入）。
2. “继续学习”入口，进入下一组。
3. 通过 `onShareAppMessage` 支持转发分享（标题含今日识字数）。

页面不展示分数、正确率、掌握状态或未完成提示。

### 6.4 随机复习页（复用学习页）

- 复习来源仅为已经在顺序识字中标记为“已学习”的文字。
- 不使用复习库、错题库、收藏或熟练度筛选。
- 每组随机复习固定为 10 个字，不提供数量选择或自定义入口。
- 已学习文字不足 10 个时，使用全部已学习文字。
- 尚未学习任何文字时显示空状态，并提供“先认识第一个字”入口（回到首页）。

每张复习卡分为两个状态。

**揭晓前：**

- 只显示彝文字形。
- 显示当前进度（进度条 + 百分比）。
- 显示提示“点击卡片查看读音与资料”。

**揭晓后：**

- 显示完整拼音（声母/韵母/声调三色）。
- 显示音调、部首、总笔画与常用词组（支持播放与换一组）。
- 未知资料统一显示“暂无资料”。

底部操作只有“上一个”和“下一个”。

复习规则：

- 每次开始复习时，从已学习文字中随机抽取 10 个（洗牌）。
- 同一轮复习内不得出现重复文字。
- 新一轮复习允许再次抽到之前复习过的文字。
- 每次进入新卡时恢复为“未揭晓”状态。
- 复习不产生正确、错误、分数、熟练度和下次复习日期。
- 完成一轮复习后累加当日回顾字数与累计回顾字数，并记为有效学习日。
- 退出后无需保存卡片位置；再次进入开始新一轮随机复习。

### 6.5 复习完成

仅展示本次回顾的文字数量和简短完成反馈，例如“已回顾 10 个彝文字”，并说明“没有分数，也不用判断对错”。操作仅为“完成回顾（返回首页）”和“再回顾一组”。不得展示正确率、薄弱文字、复习等级或待复习数量。

### 6.6 发音能力（已实现）

- 每个文字带有网络真人发音 MP3（`audioUrl`），学习页、复习页、详情页入口可播放。
- 词组发音：按词组彝文逐字取对应单字音频，优先使用 WebAudio 解码、去除首尾静音、统一采样率并拼接为 WAV 本地文件播放；WebAudio 不可用时回退为 MP3 直接拼接。合成结果按词组缓存到本地，避免重复下载。
- 播放失败时提示“音频播放失败，请检查网络”，不影响浏览。
- 反馈音：卡片切换、资料揭晓、播放/换词组时按用户设置播放本地提示音（`assets/sound/tap.wav`）与轻振动。

## 7. 字典与单字详情

### 7.1 字典 Tab

字典覆盖全部 1,164 个文字，支持：

- 精确查询：输入彝文字或完整拼音（大小写不敏感）即时检索。
- 按声母（44 项，含“无声母”）、韵母（10 项）、部首（25 项）、总笔画（1–8 画）筛选；每个维度内为单选，点击已选项可取消。
- 结果列表：显示字形、完整拼音、部首、笔画，点击进入单字详情。
- 无任何条件时为空状态提示“快来查询试试啊”；有条件下无结果提示“换一个条件试试吧”。

字典不提供收藏功能。搜索、筛选和查看详情不改变顺序识字进度。

> 注：代码中已实现 `resetConditions`（清除全部筛选）函数，但当前页面未提供对应 UI 按钮，计划在后续版本接入。

### 7.2 单字详情

单字详情展示字形、完整拼音、声母、韵母、部首、总笔画、字典序号（`N / 1,164`），以及“尚未学习”或“已学习”状态。

页面不显示收藏、所属课程、掌握状态和答题记录。

> 注：详情页包含“剩余笔画”与“词组”两行，当前实现中对应数据字段（`remainingStrokes` / `phrases`）未正确接入详情页绑定，二者恒显示“暂无资料”，后续需修复。

## 8. 我的 Tab

已实现内容：

- 累计学习字数：`已认识 X / 1,164`。
- “距离半程还差 N 字”提示（半程按 582 字计算）。
- 今日学习进度：`今日进度 X%` + 10 格进度条。
- 本周学习记录与历史最长连续天数。
- 统计：今日学习、累计回顾（复习字数）、学习天数。
- 成就：展示最新解锁的识字徽章与连续学习徽章预览，点击进入成就页。
- 声音、振动设置开关（卡片切换提示音 / 操作反馈）。
- “本地存储说明”入口。
- “清除本地学习记录”：入口位于本地存储说明页，执行前必须二次确认，并明确提示不可恢复（“识字进度、学习天数、回顾次数和成就都会删除，且无法恢复”）。

不得出现已掌握字数、课程或课节完成数、答题次数、正确率、最佳成绩、错题本和收藏列表。

## 9. 学习记录与连续天数

### 9.1 有效学习日

用户当天满足以下任一条件，即记为一个有效学习日（写入 `studyDates`）：

- 在顺序识字中首次学习至少 1 个新字。
- 完成至少一轮随机复习。

同一天重复操作不重复增加累计学习天数。

### 9.2 连续学习规则

- 昨日存在有效学习记录且今日再次学习时，连续天数加一。
- 间隔一天以上再次学习时，当前连续天数从一重新开始。
- 断签不影响累计学习天数、累计识字数和历史最长连续天数。
- 首页显示当前连续天数；当最近学习日既不是今天也不是昨天时，当前连续天数显示为 0。
- 连续学习成就按历史最长连续天数计算，一旦达成即解锁且不因断签回退。

## 10. 成就

### 10.1 成就清单（已实现，共 9 枚）

识字类：

- 初识彝文：累计学习 1 字以上。
- 小有积累：累计学习不足 100 字。
- 百字同行：累计学习 100～581 字。
- 半程同行：累计学习 582～999 字。
- 彝文传承者：累计学习 1000 字以上。

连续学习类：

- 连续学习 3 天。
- 连续学习 7 天。
- 连续学习 30 天。
- 连续学习 100 天。

成就只基于可验证的学习和使用行为，不使用“掌握”“考试通过”等表述。徽章图片为本地/远端 PNG 资源，未解锁时置灰。首次解锁时间写入 `unlockedAchievements`。

### 10.2 分享

学习完成页以海报形态展示当日成就（连续学习、累计识字、今日识字、本周记录），并支持小程序转发分享（`onShareAppMessage`）。海报不依赖收藏、课程、成绩、用户昵称或头像。分享失败、取消或相册授权失败不得影响学习记录。

> 注：海报保存（写入相册）当前仅做提示占位，未真正保存；“分享给朋友”按钮已注释，仅保留转发能力。

## 11. 本地数据模型

```ts
interface WordProgress {
  learnedAt: number
}

interface UserProgress {
  version: 2
  nextWordIndex: number
  learnedWords: Record<string, WordProgress>
  dailyLearnCounts: Record<string, number>
  dailyReviewCounts: Record<string, number>
  studyDates: string[]
  streak: number
  longestStreak: number
  totalReviewCount: number
  unlockedAchievements: Record<string, number>
  settings: {
    sound: boolean
    vibration: boolean
  }
}
```

要求（均已实现于 `miniprogram/utils/progress.ts`）：

- 使用单一版本化 Storage 根对象（`yistudy_progress`），避免多个页面分别维护冲突状态。
- `nextWordIndex` 指向字典中第一个未学习文字。
- 页面在 `onShow` 时重新读取进度，保证跨页返回后数据刷新。
- 数据迁移时只保留能够映射为“已学习”的记录（旧版按 `learnedCount` 映射为前 N 字已学习），不将旧版答题、课程或收藏状态转换为新的学习事实。
- 写入失败时提供明确反馈，不阻止用户继续浏览当前页面。

## 12. 视觉与交互原则

- 现代、简洁、友好的视觉基线，彝族红、黄、黑只作为身份色和状态点缀。
- 暖米白背景（`#FAF7F0`）、近黑正文（`#171614`）、彝红主操作（`#E13C2F`）和太阳黄成就强调（`#F2B632`）。
- 彝文字形是学习页和复习页唯一的强视觉中心。
- 完整信息通过字号、间距和分组形成层级，不将学习卡设计成密集数据表。
- 学习记忆卡为深色主卡，复习未揭晓状态只显示字形。
- 单页只保留一个最强主按钮；所有点击区域不小于 44 CSS px。
- 动效遵循微信原生组件能力，并尊重系统减少动态效果设置。
- 已通过微信开发者工具对首页、学习卡、学习完成海报、“我的”页面进行视觉还原比对（见 `design-qa.md`）。

## 13. 商业化原则

- 未达到流量主资格或留存目标前，不展示广告占位。
- 获得资格后，激励视频只能由用户主动触发，并用于非核心扩展内容。
- 顺序识字、卡片切换、随机复习和学习完成不得强制插屏。
- 广告加载失败不得阻断核心识字与字典功能。

## 14. 数据指标

首版重点关注：

- 首页到顺序识字页的转化率。
- 首次打开到第一张学习卡的耗时。
- 用户每次学习的平均字数和每日学习字数分布。
- 学习 1、5、10 个字后的退出比例。
- 次日、7 日和 30 日留存。
- 随机复习启动率和人均复习字数。
- 累计学习 10、50、100 个字的用户比例。
- 断签用户三日内再次学习的比例。

首版验证优先级为：开始识字率 > 单次学习完成度 > 次日留存 > 随机复习使用率 > 分享率。

> 注：当前为纯本地实现，尚未接入埋点统计，以上指标为产品规划目标。

## 15. 验收标准

### 15.1 数据与顺序

- 字典包含且仅包含 1,164 个唯一文字（`words.ts` 中 1,164 条）。
- 顺序识字严格按照字典顺序进行。
- 同一文字重复查看不重复增加累计学习数量。
- 退出后重新进入，从第一个未学习文字继续。
- 字典查看不会改变顺序识字位置。
- 缺部首及笔画为 `0` 的现有记录统一显示“暂无资料”（`displayText` 返回 `-`）。

### 15.2 每日识字

- 每组固定学习 10 个字，页面不存在数量选择和自定义入口。
- 最后一组不足 10 个字时使用全部剩余文字。
- 用户未完成一组也可随时退出，已学习进度正确保存。
- 学习卡完整展示规定资料，底部只有“上一个”和“下一个”。
- 学完本组 10 个字后可完成今日学习或继续下一组。
- 学完全部文字后首页和学习页进入正确完成状态。

### 15.3 随机复习

- 只从已学习文字中抽取，同一轮复习内文字不重复。
- 揭晓前只显示字形，点击后才显示完整资料。
- 复习页不出现判断按钮、分数、正确率和熟练度。
- 每组固定随机复习 10 个字，页面不存在数量选择和自定义入口。
- 已学习文字不足 10 个时使用全部已学习文字。
- 没有已学习文字时空状态和识字入口可用。

### 15.4 字典

- 字典可检索全部 1,164 个文字，且不存在收藏入口和收藏状态。
- 单字详情只显示“尚未学习”或“已学习”。

### 15.5 统计与数据

- 学习、复习、连续天数和总体进度统计一致。
- 同日重复使用不重复增加累计学习天数。
- 断签后当前连续天数重置，但累计数据和历史最长连续天数保留。
- 清除本地记录前进行二次确认，并明确提示不可恢复。
- 文档和产品中不存在课程课节、答题巩固、错题、收藏、复习队列和掌握率的残留入口。

### 15.6 工程质量

- TypeScript 类型检查通过。
- 所有 JSON 配置可解析，WXML 事件处理器和组件注册完整。
- 微信开发者工具重新编译后无新增错误或警告。
- 已完成首页、学习卡、学习完成海报、“我的”页面在微信开发者工具中的视觉还原与静态验证（见 `design-qa.md`）。

## 16. 版本优先级

### 已实现（V2.x 当前状态）

- 1,164 个文字的固定顺序识字。
- 每组固定 10 个字的顺序识字。
- 只有“上一个 / 下一个”的完整信息学习卡（含音调、词组、发音）。
- 从已学习文字中不重复抽样的随机复习（点击揭晓）。
- 全量字典（精确查询 + 声母/韵母/部首/笔画筛选）和单字详情。
- 本地学习进度、每日统计和连续学习记录。
- 学习、字典、我的三个 TabBar 页面。
- 每日学习成就海报与转发分享。
- 9 枚成就徽章（识字 5 + 连续 4）。
- 声音/振动设置与本地存储说明、清除记录。

### 待完善 / 后续增强

- 学习完成海报真正写入相册；恢复“分享给朋友”按钮。
- 字典页“清除全部筛选”UI 按钮接入。
- 单字详情页剩余笔画、词组字段正确接入展示。
- 真人音频、释义和例词补齐（当前词组为现有数据子集，音频为逐字拼接）。
- 云端同步和跨设备恢复。
- 埋点统计与上述数据指标。
- 机构合作内容和会员权益。

## 17. 代码结构索引

| 模块 | 路径 |
| --- | --- |
| 全局配置与 TabBar | `miniprogram/app.json` |
| 数据层（学习进度、成就、设置） | `miniprogram/utils/progress.ts` |
| 日期工具（有效学习日、连续、周打卡） | `miniprogram/utils/date.ts` |
| 交互反馈（声音/振动） | `miniprogram/utils/feedback.ts` |
| 字库数据（1,164 字） | `miniprogram/data/words.ts` |
| 声母/韵母/部首筛选表 | `miniprogram/data/yidict-pinyin.ts` |
| 学习/复习页 | `miniprogram/pages/learn/learn.*` |
| 首页 | `miniprogram/pages/index/index.*` |
| 完成页（海报 / 复习完成） | `miniprogram/pages/complete/complete.*` |
| 字典 | `miniprogram/pages/dictionary/dictionary.*` |
| 单字详情 | `miniprogram/pages/detail/detail.*` |
| 我的 | `miniprogram/pages/profile/profile.*` |
| 成就 | `miniprogram/pages/achievements/achievements.*` |
| 本地存储说明 | `miniprogram/pages/storage-info/storage-info.*` |
| 全局设计令牌与 UI 原语 | `miniprogram/app.scss` |
